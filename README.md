# Northstar Student Management

A simple student management dashboard built with Node.js, Express, HTML, CSS, and vanilla JavaScript.

## Run locally

```bash
npm install
npm start
```

Open http://localhost:3000 in your browser.

The app keeps data in memory, so newly added or deleted records reset when the server restarts.
